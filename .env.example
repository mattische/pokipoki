# Domain or IP for Caddy (HTTPS)
DOMAIN=pokerplanning.duckdns.org

# Server port (must match Caddyfile)
PORT=3000

# Environment
NODE_ENV=production

# JWT Secret — generate with: openssl rand -base64 32
JWT_SECRET=your-secret-here
